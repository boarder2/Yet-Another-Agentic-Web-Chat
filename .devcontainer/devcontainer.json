{
  "name": "YAAWC",
  "dockerComposeFile": ["docker-compose.devcontainer.yml"],
  "service": "app",
  "workspaceFolder": "/workspaces/YAAWC",
  "shutdownAction": "stopCompose",
  "overrideCommand": false,
  "remoteUser": "node",
  "forwardPorts": [3000, 4000],
  "portsAttributes": {
    "3000": {
      "label": "YAAWC (Next.js)"
    },
    "4000": {
      "label": "SearXNG"
    }
  },
  "postCreateCommand": "yarn install --frozen-lockfile --network-timeout 600000 && npx playwright install chromium || true",
  "customizations": {
    "vscode": {
      "settings": {
        "editor.formatOnSave": true,
        "eslint.validate": [
          "javascript",
          "javascriptreact",
          "typescript",
          "typescriptreact"
        ],
        "files.watcherExclude": {
          "**/.next/**": true,
          "**/node_modules/**": true
        },
        "terminal.integrated.defaultProfile.linux": "zsh"
      },
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "bradlc.vscode-tailwindcss"
      ]
    }
  },
  "remoteEnv": {
    "LANGSMITH_ENDPOINT": "${localEnv:LANGSMITH_ENDPOINT}",
    "LANGSMITH_API_KEY": "${localEnv:LANGSMITH_API_KEY}",
    "DISPLAY": "${localEnv:DISPLAY}",
    "XAUTHORITY": "${localEnv:XAUTHORITY}"
  }
}
