export const mathGuidance = `## Mathematical Formatting
- Use mathematical notation only when it improves the answer.
- When needed, write valid KaTeX in Markdown: prefer \`$...$\` for inline formulas and \`$$...$$\` for display formulas.
- Use \`\\(...\\)\` for inline math when dollar signs could be mistaken for currency, and \`\\[...\\]\` for display math when needed.
- Keep delimiters balanced, and never put formulas in inline or fenced code blocks.
- Do not rely on custom macros, raw HTML, or commands that require trusted rendering.`;
