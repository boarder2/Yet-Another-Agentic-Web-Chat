[GENERAL]
SIMILARITY_MEASURE = "cosine"
KEEP_ALIVE = "5m"
BASE_URL = ""
PRIVATE_SESSION_DURATION_MINUTES = 1440
# HIDDEN_MODELS and [GENERAL.RETENTION] have been migrated to the database
# (app_settings table). Manage them from Settings → Model Visibility / Retention.
# Selected models (embedding + memory-processing model) are likewise DB-backed
# and configured from the Settings UI.

[MODELS.OPENAI]
API_KEY = ""

[MODELS.GROQ]
API_KEY = ""

[MODELS.OPENROUTER]
API_KEY = ""

[MODELS.ANTHROPIC]
API_KEY = ""

[MODELS.GEMINI]
API_KEY = ""

[MODELS.CUSTOM_OPENAI]
API_KEY = ""
API_URL = ""
MODEL_NAME = ""

[MODELS.OLLAMA]
API_URL = ""

[MODELS.DEEPSEEK]
API_KEY = ""

[MODELS.AIMLAPI]
API_KEY = ""

[MODELS.LM_STUDIO]
API_URL = ""

[API_ENDPOINTS]
SEARXNG = ""

[SEARCH]
# Search provider + locale preferences (PROVIDER, PRIVATE_PROVIDER,
# FALLBACK_PROVIDER, LANGUAGE, REGION) have been migrated to the database
# (app_settings table). Manage them from Settings → Search Providers.
# Provider credentials/endpoints below stay here (secrets/infra).

[SEARCH.PROVIDERS.SEARXNG]
API_URL = ""

[SEARCH.PROVIDERS.BRAVE_SEARCH]
API_KEY = ""

[SEARCH.PROVIDERS.BRAVE_LLM]
API_KEY = ""

[SEARCH.PROVIDERS.MOJEEK]
API_KEY = ""

[TOOLS.CODE_EXECUTION]
ENABLED = false
DOCKER_IMAGE = "node:slim"
DOCKER_HOST = "unix:///var/run/docker.sock"
TIMEOUT_SECONDS = 30
MEMORY_MB = 128
MAX_OUTPUT_CHARS = 50000

# [TOOLS.IMAGE_GENERATION] has been migrated to the database (app_settings
# table). Manage it from Settings → Image Generation. The OpenRouter API key it
# uses stays in [MODELS.OPENROUTER].
